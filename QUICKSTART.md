# Quick Start Guide

## 🚀 Выберите способ запуска

### Вариант A: VPS (рекомендуется для production)
⏱️ **Время: 5 минут**

### Вариант B: Локально (для разработки)
⏱️ **Время: 10-15 минут**

---

## 🌐 Вариант A: Развертывание на VPS

### Шаг 1: Получите VPS

**Рекомендуемые провайдеры:**
- [Hetzner Cloud](https://www.hetzner.com/cloud) - €4.15/мес (2GB RAM)
- [DigitalOcean](https://www.digitalocean.com) - $6/мес (1GB RAM)
- [Linode](https://www.linode.com) - $5/мес (1GB RAM)

**Минимальные требования:**
- Ubuntu 20.04/22.04
- 1 vCPU, 1GB RAM
- 10GB диск

### Шаг 2: Подключитесь к VPS

```bash
ssh root@your-vps-ip
```

### Шаг 3: Загрузите проект

**Способ 1: Скачать с GitHub**
```bash
git clone https://github.com/your-username/teamflow-mvp.git
cd teamflow-mvp
```

**Способ 2: Загрузить архив**
```bash
# На вашем компьютере:
scp teamflow-mvp.tar.gz root@your-vps-ip:/root/

# На VPS:
tar -xzf teamflow-mvp.tar.gz
cd teamflow-mvp
```

### Шаг 4: Запустите автоматический деплой

```bash
chmod +x deploy-vps.sh
./deploy-vps.sh
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Настроит firewall
- ✅ Запросит Telegram токены
- ✅ Запустит приложение
- ✅ Настроит автозапуск
- ✅ Настроит ежедневные бэкапы

### Шаг 5: Готово!

Откройте в браузере:
```
http://your-vps-ip:5173
```

Протестируйте бота в Telegram:
```
/task
/week
/digest
```

---

## 💻 Вариант B: Локальный запуск

### Шаг 1: Установите Docker

**macOS:**
```bash
brew install --cask docker
# Или скачайте Docker Desktop с docker.com
```

**Windows:**
```powershell
# Скачайте Docker Desktop с docker.com
# Убедитесь что WSL2 установлен
```

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

### Шаг 2: Распакуйте проект

```bash
tar -xzf teamflow-mvp.tar.gz
cd teamflow-mvp
```

### Шаг 3: Настройте Telegram бота

#### 3.1 Создайте бота

1. Откройте Telegram → найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (выглядит как `123456:ABC-DEF1234ghIkl...`)

#### 3.2 Получите Chat ID

1. Создайте групповой чат в Telegram
2. Добавьте бота в чат
3. Отправьте любое сообщение
4. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
5. Найдите `"chat":{"id":-1001234567890}`
6. Скопируйте Chat ID (отрицательное число)

#### 3.3 Отключите Privacy Mode

1. @BotFather → `/mybots`
2. Выберите вашего бота
3. Bot Settings → Group Privacy → **Turn off**

### Шаг 4: Создайте .env файл

```bash
cp backend/.env.example backend/.env
```

Отредактируйте `backend/.env`:
```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID=-1001234567890
```

### Шаг 5: Запустите приложение

```bash
docker-compose up --build
```

Подождите пока контейнеры запустятся (~1-2 минуты)

### Шаг 6: Откройте Web UI

```
http://localhost:5173
```

### Шаг 7: Протестируйте бота

Отправьте в Telegram чат:
```
/task
```

Бот должен ответить!

---

## 📱 Основные команды

После запуска используйте эти команды в Telegram:

### Создание задач
```
/task                  # Создать задачу через диалог
```

Или просто напишите в чат:
```
@john нужно проверить API до пятницы
```
Бот автоматически предложит создать задачу!

### Просмотр доски
```
/week                  # Показать недельную доску
```

### Встречи
```
/meeting               # Зафиксировать результаты встречи
/meetings              # Показать историю встреч
```

### Аналитика
```
/digest                # Еженедельный дайджест
/overdue               # Просроченные задачи
```

---

## 🔧 Управление приложением

### Просмотр логов
```bash
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Остановка
```bash
docker-compose down
```

### Перезапуск
```bash
docker-compose restart
```

### Обновление (после git pull)
```bash
docker-compose up --build -d
```

### Резервное копирование
```bash
# Локально
docker cp teamflow-backend:/app/data/teamflow.db ./backup.db

# На VPS (автоматически настроено)
/opt/teamflow/backup.sh
```

---

## 🐛 Troubleshooting

### Бот не отвечает

**Проверьте:**
1. Правильность токена в `.env`
2. Chat ID (должен быть отрицательным числом)
3. Privacy Mode выключен у бота
4. Бот добавлен в чат

**Логи:**
```bash
docker-compose logs backend | grep -i error
```

### Порты заняты

Если порты 5173 или 8000 заняты, измените в `docker-compose.yml`:
```yaml
ports:
  - "8080:5173"  # Вместо 5173:5173
  - "9000:8000"  # Вместо 8000:8000
```

### База данных не создается

```bash
# Пересоздайте volume
docker-compose down -v
docker-compose up --build
```

### Медленный запуск локально

**Причина:** Первая сборка образов

**Решения:**
1. Используйте VPS (уже собранные образы)
2. Увеличьте ресурсы Docker Desktop
3. Дождитесь первой сборки (следующие запуски будут быстрее)

**Настройка Docker Desktop:**
- macOS/Windows: Settings → Resources → увеличьте CPU и Memory

---

## 🎯 Что дальше?

### Для разработки
1. Изучите структуру кода в `/backend/app`
2. Прочитайте документацию в `/docs`
3. Следуйте coding standards в `docs/20-coding-standards.md`

### Для production
1. Настройте домен и SSL (см. `VPS_DEPLOY.md`)
2. Настройте автоматические бэкапы
3. Настройте мониторинг

### Для команды
1. Пригласите участников в Telegram чат
2. Объясните основные команды
3. Начните фиксировать задачи!

---

## 📞 Помощь

**Проблемы с развертыванием?**
- См. подробную инструкцию: `VPS_DEPLOY.md`
- Проверьте логи: `docker-compose logs`

**Вопросы по использованию?**
- Список команд: `/help` в боте
- Документация: `/docs` в проекте

**Нашли баг?**
- Создайте Issue на GitHub
- Приложите логи

---

## ⚡ TL;DR - Самый быстрый способ

### На VPS:
```bash
ssh root@your-vps-ip
curl -sSL https://raw.githubusercontent.com/your/teamflow/main/deploy-vps.sh | bash
```

### Локально:
```bash
tar -xzf teamflow-mvp.tar.gz && cd teamflow-mvp
cp backend/.env.example backend/.env
# Отредактируйте .env с вашими токенами
docker-compose up --build
```

**Готово!** 🎉
