# TeamFlow - Team Task Management via Telegram

**TeamFlow** — локальный инструмент фиксации договорённостей команды через Telegram с веб-просмотром состояния задач.

Проект предназначен для маленьких команд (3–5 человек), работающих без формальных процессов.

## 🎯 Основные цели

- ✅ Фиксировать задачи, озвученные в чате
- ✅ Хранить недельные договорённости
- ✅ Видеть текущий статус работы
- ✅ Не внедрять таск-трекер
- ✅ Работать локально без внешних сервисов

## 🚀 Основные интерфейсы

### 1. Telegram Bot — основной интерфейс работы
- Команды для создания задач
- Inline кнопки для быстрых действий
- Автоматический парсинг сообщений чата
- Недельная доска задач

### 2. Web UI — read-only обзор состояния
- Просмотр всех задач
- Фильтрация по статусам
- История встреч
- Блокеры задач

## 📋 Возможности

### Управление задачами
- ✅ Создание через команду `/task`
- ✅ **Автоматическое создание из сообщений чата** (NEW!)
- ✅ Статусы: TODO, DOING, DONE, BLOCKED
- ✅ Назначение исполнителей через @username
- ✅ Сроки выполнения (due date)
- ✅ Definition of Done для каждой задачи
- ✅ Inline кнопки для быстрой смены статуса

### Блокеры
- ✅ Отметка задачи как заблокированной
- ✅ Указание причины блокировки
- ✅ История блокеров

### Встречи
- ✅ Фиксация результатов встреч (`/meeting`)
- ✅ Краткое резюме договорённостей
- ✅ Просмотр истории (`/meetings`)

### Доска задач
- ✅ Команда `/week` для просмотра текущей недели
- ✅ Группировка по статусам
- ✅ Inline кнопки для смены статуса
- ✅ Улучшенное форматирование через BoardService

### Дайджесты и аналитика (NEW!)
- ✅ Еженедельный дайджест (`/digest`)
- ✅ Статистика выполнения
- ✅ Активность команды
- ✅ Напоминания о просроченных задачах (`/overdue`)

## 🛠️ Технологический стек

### Backend
- **Python 3.11+**
- **aiogram 3** - Telegram Bot framework
- **FastAPI** - Web API
- **SQLAlchemy** - ORM
- **SQLite** - база данных (для простоты локального деплоя)
- **Pydantic** - валидация данных

### Frontend
- **React 18** с TypeScript
- **Vite** - сборщик
- **TanStack Query** - управление данными
- **Tailwind CSS** - стилизация

### DevOps
- **Docker & Docker Compose**
- Единый контейнер для бота и API

## 📁 Архитектура

### Архитектурный стиль
**Modular Monolith** с разделением на слои:

```
Telegram Chat 
    ↓
Telegram Bot (aiogram)
    ↓
Application Services
    ↓
Repository Layer
    ↓
SQLite Database
    ↓
Web API (FastAPI)
    ↓
Web UI (React)
```

### Структура проекта

```
teamflow-mvp/
├── backend/
│   ├── app/
│   │   ├── main.py           # Точка входа (Bot + API)
│   │   ├── config.py         # Конфигурация
│   │   │
│   │   ├── core/            # Общие утилиты
│   │   │   ├── db.py        # SQLite подключение
│   │   │   ├── logging.py   # Структурное логирование
│   │   │   └── clock.py     # Работа со временем
│   │   │
│   │   ├── domain/          # Доменные модели
│   │   │   ├── models.py    # Task, Blocker, Meeting
│   │   │   ├── enums.py     # TaskStatus, TaskSource
│   │   │   └── events.py    # Domain events
│   │   │
│   │   ├── repositories/    # Data Access Layer
│   │   │   ├── task_repository.py
│   │   │   ├── blocker_repository.py
│   │   │   └── meeting_repository.py
│   │   │
│   │   ├── services/        # Application Services
│   │   │   ├── task_service.py
│   │   │   ├── board_service.py
│   │   │   └── message_parsing_service.py
│   │   │
│   │   ├── telegram/        # Telegram Bot
│   │   │   ├── bot.py       # Bot setup
│   │   │   ├── handlers/    # Command handlers
│   │   │   ├── keyboards/   # Inline keyboards
│   │   │   └── fsm/         # Finite State Machines
│   │   │
│   │   └── web/             # Web API
│   │       ├── app.py       # FastAPI app
│   │       ├── routes.py    # REST endpoints
│   │       └── schemas.py   # Pydantic schemas
│   │
│   ├── migrations/          # Alembic migrations
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                # React Web UI
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   └── api/
│   ├── package.json
│   └── Dockerfile
│
├── docs/                    # Документация
│   ├── 01-overview.md
│   ├── 02-architecture.md
│   └── ...
│
├── docker-compose.yml
└── README.md
```

## 🚀 Быстрый старт

### Предварительные требования

1. **Docker и Docker Compose**
2. **Telegram Bot Token** от @BotFather
3. **Chat ID** вашего Telegram чата

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd teamflow-mvp
```

2. **Создайте .env файл**
```bash
cp backend/.env.example backend/.env
```

3. **Настройте переменные окружения**
```env
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Database
DATABASE_URL=sqlite:///./teamflow.db

# Web API
API_HOST=0.0.0.0
API_PORT=8000
```

4. **Запустите приложение**
```bash
docker-compose up --build
```

5. **Откройте Web UI**
```
http://localhost:5173
```

### Настройка Telegram бота

1. Создайте бота через @BotFather
2. Получите токен
3. Добавьте бота в ваш групповой чат
4. **Важно:** Отключите Privacy Mode в настройках бота через @BotFather:
   - `/mybots` → выберите бота → Bot Settings → Group Privacy → Turn off
5. Получите Chat ID:
```bash
# Отправьте сообщение в чат с ботом, затем:
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```

## 📱 Использование Telegram бота

### Команды

#### `/task` - Создание задачи
Запускает интерактивный диалог создания задачи:
1. Введите название задачи
2. Опционально: описание
3. Опционально: исполнитель (@username)
4. Опционально: срок выполнения
5. Опционально: Definition of Done

#### `/week` - Недельная доска
Показывает все задачи текущей недели, сгруппированные по статусам:
- 📝 TODO
- 🔄 DOING
- ✅ DONE
- 🚫 BLOCKED

С inline кнопками для быстрого изменения статуса.

#### `/meeting` - Фиксация встречи (NEW!)
Запись результатов встречи команды:
1. Введите краткое резюме договорённостей
2. Встреча автоматически сохранится с датой и временем

#### `/meetings` - История встреч (NEW!)
Показывает все встречи за последний месяц с их резюме.

#### `/digest` - Еженедельный дайджест (NEW!)
Генерирует подробный отчет за неделю:
- 📈 Статистика выполнения задач
- ✅ Список выполненных задач
- ⚠️ Заблокированные задачи
- 🤝 Проведенные встречи
- 👥 Активность команды

#### `/overdue` - Просроченные задачи (NEW!)
Показывает список задач с истекшим сроком выполнения.

### Автоматический парсинг сообщений

Бот автоматически анализирует сообщения в чате и предлагает создать задачу, если обнаруживает:
- Упоминание исполнителя (@username)
- Указание срока (завтра, в пятницу, через 3 дня)
- Ключевые слова задач (сделать, реализовать, проверить)

**Пример:**
```
@john нужно проверить API до пятницы
```
↓
```
Бот: Создать задачу?
📋 Проверить API
👤 @john
📅 15.02.2025

[Да] [Редактировать] [Нет]
```

### Inline действия

Под каждой задачей в доске `/week` есть кнопки:
- **▶️ Start** - перевести в DOING
- **✅ Done** - перевести в DONE
- **🚫 Block** - отметить как заблокированную
- **📝 Edit** - редактировать детали

## 🌐 Web UI (Read-Only)

### Доступные страницы

#### Dashboard
- Обзор всех задач
- Фильтры по статусам
- Поиск по названию
- Группировка по исполнителям

#### Task Details
- Полная информация о задаче
- История изменений
- Список блокеров
- Ссылка на исходное сообщение в Telegram

#### Meetings
- История встреч команды
- Резюме договорённостей
- Связанные задачи

## 🗄️ База данных

### Schema

#### tasks
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT,
    assignee_name TEXT,
    assignee_telegram_id INTEGER,
    status TEXT NOT NULL,  -- TODO, DOING, DONE, BLOCKED
    due_date TEXT,
    definition_of_done TEXT,
    source TEXT NOT NULL,  -- MANUAL_COMMAND, CHAT_MESSAGE
    source_message_id INTEGER,
    source_chat_id INTEGER,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
```

#### blockers
```sql
CREATE TABLE blockers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER NOT NULL,
    text TEXT NOT NULL,
    created_by INTEGER,
    created_at TEXT NOT NULL,
    FOREIGN KEY(task_id) REFERENCES tasks(id)
);
```

#### meetings
```sql
CREATE TABLE meetings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    meeting_date TEXT NOT NULL,
    summary TEXT NOT NULL,
    created_at TEXT NOT NULL
);
```

## 🔧 API Endpoints (Read-Only)

```
GET  /api/tasks              - Список задач
GET  /api/tasks/{id}         - Детали задачи
GET  /api/tasks/week         - Задачи текущей недели
GET  /api/blockers/{task_id} - Блокеры задачи
GET  /api/meetings           - История встреч
GET  /api/stats              - Статистика
```

## 🏗️ Архитектурные принципы

1. **Единый источник истины** - SQLite база
2. **Бизнес-логика не зависит от Telegram** - сервисы переиспользуются
3. **Web и Bot используют один сервисный слой**
4. **Минимальная инфраструктура** - один контейнер
5. **Domain-Driven Design** - чистые доменные модели
6. **Repository Pattern** - абстракция доступа к данным

## 📊 Domain Events

Система генерирует события для отслеживания изменений:

```python
# domain/events.py
- TaskCreated
- TaskStatusChanged
- TaskBlocked
- TaskUnblocked
- MeetingRecorded
```

Использование:
- Логирование истории
- Уведомления
- Аналитика

## 🔐 Безопасность

- ✅ Telegram Bot работает только с указанным Chat ID
- ✅ Web UI read-only (нет изменений через веб)
- ✅ Локальное хранение данных
- ✅ Нет внешних API вызовов
- ✅ SQLite с правами доступа

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
pytest

# Линтинг
ruff check .
mypy .
```

## 📈 Roadmap

### MVP (текущая версия)
- ✅ Telegram Bot с командами
- ✅ Автопарсинг сообщений
- ✅ CRUD задач
- ✅ Статусы и блокеры
- ✅ Read-only Web UI
- ✅ SQLite хранилище

### Фаза 2
- [ ] Напоминания о сроках
- [ ] Еженедельный дайджест
- [ ] Экспорт в Markdown
- [ ] Голосовые сообщения → задачи

### Фаза 3
- [ ] Метрики команды
- [ ] Burndown charts
- [ ] Интеграция с календарём
- [ ] Подзадачи

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

## 📝 Coding Standards

- Python: следуем PEP 8, используем type hints
- Imports: абсолютные импорты
- Docstrings: Google style
- Commits: Conventional Commits

См. подробнее в [docs/20-coding-standards.md](docs/20-coding-standards.md)

## 🐛 Отладка

### Логи
```bash
# Логи бота
docker-compose logs -f backend

# Логи Web UI
docker-compose logs -f frontend
```

### База данных
```bash
# Подключение к SQLite
docker exec -it teamflow-backend sqlite3 /app/teamflow.db

# Просмотр задач
sqlite> SELECT * FROM tasks;
```

## 📄 Документация

Полная документация в папке `/docs`:
- [01-overview.md](docs/01-overview.md) - Обзор проекта
- [02-architecture.md](docs/02-architecture.md) - Архитектура
- [03-domain-model.md](docs/03-domain-model.md) - Доменная модель
- [06-telegram-integration.md](docs/06-telegram-integration.md) - Telegram интеграция
- [11-package-structure.md](docs/11-package-structure.md) - Структура кода

И ещё 19 документов с детальным описанием каждого аспекта.

## 📞 Поддержка

Для вопросов и предложений создавайте Issues на GitHub.

---

**Разработано с ❤️ для команд, которые не хотят внедрять таск-трекер**
